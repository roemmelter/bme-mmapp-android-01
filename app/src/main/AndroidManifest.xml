<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mmapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApplication">
        <activity android:name=".NumpadDemoActivity"></activity>
        <activity
            android:name=".RotationActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"></activity>
        <activity android:name=".NumpadActivity" />
        <activity android:name=".RecordVideoActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".BarcodeActivity" />
        <activity android:name=".EmailActivity" />
        <activity android:name=".SMSActivity" />
        <activity android:name=".GeoActivity" />
        <activity android:name=".StateSwitchingActivity" />
        <activity android:name=".ButtonActivity" />
        <activity android:name=".SimpleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>